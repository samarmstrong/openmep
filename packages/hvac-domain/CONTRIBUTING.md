# Contributing

Please keep the package dependency-free at runtime, preserve deterministic sorted outputs, and add Vitest coverage for every behavior change. Run the package build and test scripts before opening a pull request.

Changes that expand system scope (for example rectangular sizing or hydraulic analysis) require an explicit API proposal and limitations update.
