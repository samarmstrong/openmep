# Contributing

Keep fixtures synthetic and non-IFC-derived. Every fixture change must update `fixtures/manifest.sha256`, preserve deterministic key ordering, and add a test for its intended grading behavior. The public schema is structural; do not import private application schemas.
